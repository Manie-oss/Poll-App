export enum tokenTypes {
    ACCESS = 'access',
    REFRESH = 'refresh',
    RESET_PASSWORD ='resetPassword',
}

export enum pollVisibility {
    PUBLIC = 'public',
    PRIVATE = 'private'
}

export enum pollStatus {
    ACTIVE = 'active',
    CLOSED = 'closed'
}